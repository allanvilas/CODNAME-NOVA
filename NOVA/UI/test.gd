extends NinePatchRect

#onready var map = $map

# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	map.set_text("Mouse pos: "+str(get_local_mouse_position()))
#	pass
